# This directory holds TLS certificates generated by scripts/gen_certs.sh.
# The actual .crt and .key files are excluded from version control (.gitignore).
# Run `./scripts/gen_certs.sh <YOUR_IP>` to populate this directory.
